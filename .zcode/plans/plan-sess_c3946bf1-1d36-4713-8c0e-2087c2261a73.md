## Approved Work Scope

### Faster regeneration and list pages
1. Parallelize the homepage’s independent data calls (`settings`, `sections`, and post summaries) and memoize `fetchSections` so the root layout and homepage do not issue the same query twice during one render.
2. Add a Supabase migration with:
   - a RLS-respecting `security_invoker` public summary view that exposes only card/list fields;
   - a public-slug JSONB expression index;
   - an RLS-respecting one-post RPC for detail routes.
3. Update `fetchPostSummaries` to use the lightweight summary view; list/discovery/search/sitemap/tool/tag/section routes will stop loading full prompts, galleries, and every approved comment.
4. Update `getPostBySlugOrId` to query only the requested post and only that post’s approved comments. Preserve full detail-page behavior, public visibility rules, and existing sanitization.
5. Switch RSS to the summary source because it uses only list-level fields.

### On-demand revalidation verification
6. Build and deploy the optimized Worker.
7. Perform the exact production test you reported: record the current `heroEnabled` setting, toggle it through the admin API, fetch the live homepage until the hero state changes, restore the original setting, and verify the restored homepage state. This validates the cache-interception/purge configuration end to end, not just ISR headers.

### Safety and verification
- The Supabase query/view/RPC will use `security_invoker`, preserving current RLS instead of exposing drafts or private posts.
- I will validate migration compatibility against existing JSONB values before applying it to production.
- Run lint, TypeScript validation, and OpenNext build before deployment.
- Confirm no data model/admin UI behavior changes and report before/after timing for fresh regenerations.
